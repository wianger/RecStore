interface:
  display_name: "RDMA Module"
  short_description: "Validate and benchmark RecStore RDMA PS paths"
  default_prompt: "Use $rdma-module to validate RecStore RDMA correctness, run RC transport benchmarks, and summarize RDMA failures or bottlenecks."
