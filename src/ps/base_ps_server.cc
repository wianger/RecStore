#include <cstdint>
#include <future>
#include <string>
#include <vector>

#include "base/base.h"
#include "base/array.h"
#include "base/timer.h"



int main(int argc, char **argv) {
  folly::Init(&argc, &argv);

  return 0;
}