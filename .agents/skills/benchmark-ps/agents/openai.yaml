interface:
  display_name: "Benchmark PS"
  short_description: "Run RecStore PS/network transport benchmarks"
  default_prompt: "Use $benchmark-ps to run RecStore PS/network transport benchmarks across rdma, grpc, and brpc."
